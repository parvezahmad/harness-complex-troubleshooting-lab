apiVersion: v1
kind: Service
metadata:
  name: harness-multi-infra-web
spec:
  selector:
    app: harness-multi-infra-web
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
